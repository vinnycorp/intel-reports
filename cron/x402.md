# x402 Report — Cron / Invocation

Run with:

```
node reports/x402/collect.js
```

from the repo root (`intel-reports/`).

## Suggested cron schedule

```cron
0 7 * * * cd /path/to/intel-reports && node reports/x402/collect.js
```

Runs daily at 07:00 UTC. Adjust path and time as needed.
